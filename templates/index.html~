{% extends "layout.html" %}

{% block title %}
    My Portfolio
{% endblock %}

{% block main %}
<h2> Portfolio </h2>
<div class="container-fluid">
  <div class="row">
    <table class="table table-hover">
      <tr>
	<th class="text-center">Symbol</th>
	<th class="text-center">Stock</th>
	<th class="text-center">Shares</th>
	<th class="text-center">Price</th>
      </tr>
      {% for stock, info in portfolio.items() %}
      <tr>
	<td class="text-center"><a href="/quote?stock={{stock}}">{{ stock }}</a></th>
	<td class="text-center">{{ info["name"] }}</th>
	<td class="text-center">{{ info["shares"] }}</th>
	<td class="text-center">${{ info["price"] }}</th>
      </tr>
      {% endfor %}	
    </table>
  </div>
  <div class="row">
    Total Value = {{ total_value }}
  </div>
  <div class="row">
    Cash Balance = {{ balance }}
  </div>
</div> 
{% endblock %}
